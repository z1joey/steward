# Backend (FastAPI) — prefix STEWARDS_ (see backend/app/core/config.py)
STEWARDS_DATABASE_URL=postgresql+psycopg://steward:steward@localhost:5432/steward
STEWARDS_JWT_SECRET=dev-only-change-me-0123456789abcdef

# Frontend (Vite dev server) — base URL of the backend API
VITE_API_BASE=http://localhost:8000
