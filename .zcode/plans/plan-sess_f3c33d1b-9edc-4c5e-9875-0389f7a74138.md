## Goal

Execute `docs/development/testing.md` for branch `feature/TA01-12_AC-AUTH-INV-TR-ISO` (Tasks TA-01~TA-12). In-scope spec rows: **T-AUTH-01~03, T-INV-01~04, T-TR-01~03, T-ISO-01/02**, plus testing.md §4 role-deny matrix (branch-relevant ops: `invite.create`, `transfer.create`). The backend API cases are mostly covered by existing tests (TA-T0~T2 commits); I will run them, close the spec gaps, add Playwright e2e for the branch's UI scope (login/register/onboarding/pending-list/store-picker), and deliver a traceability report.

## Step 1 — Environment + baseline

- Local Postgres is already running (Docker is off — not needed). Create `steward_test` and a dedicated `steward_e2e` DB via `createdb` if missing (e2e writes real rows, so it gets its own DB instead of the dev `steward` DB).
- `cd backend && uv sync`, then `uv run pytest -v` → confirm existing suite (test_auth, test_invites, test_transfers, test_settings_members) is green as the baseline.

## Step 2 — Backend gap tests (fill testing.md gaps, follow existing style in backend/tests/)

1. **T-TR-02 双会话并发接受** (`tests/test_concurrency.py`, new): the current double-accept test shares one connection (savepoints). Add a true two-session test: a dedicated app instance whose `get_db` override binds real committed sessions to the `engine` fixture, two httpx clients ("双会话"), seed via API, then `asyncio.gather` two accepts of the same transfer. Routers are sync `def` → threadpool → genuine parallel transactions. Assert: exactly one 200, other 409; exactly 1 manager; original demoted; cleanup rows in `finally`.
2. **version_conflict (T-CON-01 mapping)**: invite accept and transfer accept with stale `version` → 409 `version_conflict`, zero side effects.
3. **Invite reject** (implemented, untested): reject → 200 `rejected`; accept-after-reject → 409 `already_processed`; non-invitee reject → 403 `not_invitee`.
4. **§4 deny matrix** (`tests/test_role_matrix.py`, new): parametrize over branch-available manager-only ops — `invite.create`, `transfer.create`: User_SM → 403 `forbidden_role`; User_M → allow. (Matrix extends as later modules land.)
5. **Edge codes**: `self_transfer` → 422; path `store_id` ≠ `X-Store-Id` → 404; `GET /me` without token → 401.

Re-run full suite → all green.

## Step 3 — Frontend e2e (Playwright; chromium already installed)

1. `frontend/playwright.config.ts`: add chromium project + `webServer` array so `npm run e2e` is self-contained: backend `uvicorn` on :8000 with `STEWARDS_DATABASE_URL` → `steward_e2e` (after alembic upgrade), Vite dev on :8080; keep `E2E_BASE_URL` override. Assertions use `COPY` from `src/shared/copy.ts` per its header contract.
2. `e2e/auth.spec.ts` — T-AUTH-01 UI (register → login; wrong creds → 「手机号或密码错误」; dup phone → 「该手机号已注册」), T-AUTH-02 (no-store user auto-lands /onboarding: only 创建门店 + 待处理邀请, no ledger nav), T-AUTH-03 (create-store modal → lands /ledger/entries, manager), AC-AUTH-05 (StoreSelect hidden with single store).
3. `e2e/invites-transfer.spec.ts` (seed via Playwright `request` against the API) — T-INV-01/02 (invitee sees pending invite, accepts → store appears), reject path, transfer accept via PendingList 转让 section (transfer-create is API-only on this branch), T-CON-02 (click accept after API-side accept → 409 → toast 「已被别人更新，已刷新」), AC-INV-05 (店长 has no invite UI entry; /settings/members guard redirects).

Run `npm run e2e` → green.

## Step 4 — Report + commit

- Final traceability report (in chat): each in-scope spec ID → test file/name → pass/fail.
- Explicitly deferred (not implementable on this branch): T-ISO-02 full data isolation (ledger/employee/overview endpoints don't exist yet), T-CON-03 (claims module), transfer time-based expiry (not implemented — [O-08]; "expiry" = initiator no longer manager), §4 rows for modules not yet built.
- Commit new tests + config to the branch: `test(TA-T3): T-TR-02 dual-session concurrency, version/reject/deny-matrix gaps; e2e auth+invite+transfer flows`.

## Verification

- `uv run pytest -v` (backend) and `npm run e2e` (frontend) both fully green before reporting.
